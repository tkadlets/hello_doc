# Restricting access to two-step verification setup

## Feature related UI texts

[For reference](https://monosnap.ai/file/jZxouLF9U9nvQkLh6K1Qq9dT6LpP4U)

| \# | Initial copy | Edited copy |
| :---- | :---- | :---- |
| 1\. | Limited access to setting up | Access restriction to two-step verification setup   |
| 2\. | Allow only approved users to set up two-step verification  | Require approval to set up two-step verification |
| 3\. | User with unconfigured two-step verification will be able to log in after approval only. | Users who don’t have two-step verification configured will be prompted to request approval when trying to sign in. They will not be able to log in until approved. |
| 4\. | Add the users you want to allow setting up of two-step verification or wait request for access. Approval is valid for N days after that access will be reset. | Add users to grant them access to set up two‑factor verification, or approve incoming access requests. Approvals expire after N days.  |
| 5\. | Approved users (0) // Approve pending (0) | Approved users // Pending requests |
| 6\. | Specify the users who are allowed to set up two-step verification | Select users permitted to set up two-step verification. |
| 7\. | Enabled | Enabled for all users |
| 8\. | Number of exclusion | Excluded users: 3 |
| 9\. | Limited access to setting up enabled | Restricted access to setup is enabled |
| 10\. | Allowed users: 9 // Approve pending: 0  | Approved users: 0 // Pending approval: 0 |
| 11\. | Login denied. The access request is sent to system administrator. | Access denied. Request sent to system administrator. |
| 12\. | You have 1 request to login and set up two-factor authentication | You have 1 pending request to log in and set up two-factor verification. |